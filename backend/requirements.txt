fastapi
uvicorn[standard]
sqlalchemy
alembic
psycopg2-binary
pydantic
# Backs pydantic's EmailStr, which is an optional extra rather than part of
# pydantic itself. Used by the register/login schemas (T-AUTH-3) so "valid
# email" is one maintained implementation instead of a hand-rolled regex.
email-validator
pydantic-settings
python-dotenv
# Password hashing. Used directly rather than via passlib[bcrypt]: passlib
# 1.7.4 (2020, unmaintained) fails on every hash against bcrypt >= 4.1 — see
# the module docstring in app/core/security.py.
bcrypt
# Access-token encode/decode.
pyjwt
# The chat assistant's Messages API client (T-CHAT-1). Imported from exactly
# one module, app/chat/anthropic_client.py, and never at import time from
# app/chat/client.py — so the rest of the chat feature, and its tests, run
# without it. `AnthropicFoundry` (needed by this deployment, see that module)
# ships in the same package; there is no separate Foundry distribution.
anthropic
pytest
# Required by fastapi.testclient / starlette.testclient. Starlette 1.x asks for
# httpx2 specifically (httpx 2.x, distributed under a new package name), not
# the older `httpx` distribution — installing that one leaves TestClient
# raising "requires the httpx2 package".
httpx2
