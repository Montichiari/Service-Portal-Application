# pytest is run from the backend/ directory: `cd backend && python -m pytest`.
# `pythonpath = .` puts backend/ on sys.path so tests can `import app.*`
# regardless of how pytest is invoked.
[pytest]
pythonpath = .
testpaths = tests
